var POS_X = 0,
    POS_Y = 0,
    POS_Z = 0,
    FOV = 60,
    WIDTH = window.innerWidth,
    HEIGHT = window.innerHeight,
    NEAR = 1,
	FAR = 125000,
	renderer = new THREE.WebGLRenderer({antialias:true}),
    scene = new THREE.Scene(),
    loader = new THREE.TextureLoader(),
    player = document.getElementById('musicPlayer'),
	context = new (window.AudioContext || window.webkitAudioContext),
	analyser = context.createAnalyser(),
	source = context.createMediaElementSource(player),
	canvas = document.getElementById("canvas"),
	menu = document.getElementById("menu"),
	camera = new THREE.PerspectiveCamera(FOV,WIDTH/HEIGHT,NEAR,FAR),
	controls = new THREE.OrbitControls(camera),
	uniforms,
	glow = null,
	blockGeom = new THREE.SphereBufferGeometry(300, 32, 32),
	displacement = new Float32Array(blockGeom.attributes.position.count),
	noise = new Float32Array(blockGeom.attributes.position.count),
	block = null,
	blockArray = [],
	blockXPosArray = [],
	blockYPosArray = [],
	lightArray = [],
	glowArray = [],	
	glowGeom = new THREE.SphereGeometry(400, 32, 32),
	healthGeom = new THREE.SphereGeometry(50, 6, 6),
	clock = new THREE.Clock(),
	frequencyData = null,
	paused = false,
	num = 0,
	lightNum = 0,
	glowNum = 0,
	octree = null,
	stunned = false,
	health = 1.0,
	inverseHealth = 1.0 - health,
	gameOver = false,
	angle = 0,
	healthLightsOn = false,
	songLength = null,
	spriteTexture = loader.load( "dot.png" ),
    spriteMat = new THREE.SpriteMaterial( { map: spriteTexture, color: 0xffffff, fog: true } ),
    mobile = false,
    vr = false,
    gameStart = false,
    lightCollision = false,
    lightCollisionIndex = null,
    songTimer = null;